{
  "TCP": {
    "443": {
      "HTTPS": true
    }
  },
  "Web": {
    "${TS_HOSTNAME}.${TS_TAILNET}:443": {
      "Handlers": {
        "/": {
          "Proxy": "http://${TARGET_HOST}:${TARGET_PORT}"
        }
      }
    }
  }
}